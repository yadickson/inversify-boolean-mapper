name: NodeJS Build Process

on:
  push:
    branches:
      - "*"
  pull_request:
    branches:
      - "*"

jobs:    

  integration:
    uses: yadickson/workflows/.github/workflows/integration.yml@main
    secrets: inherit
    with:
      skip-mutation: true
